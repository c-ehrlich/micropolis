{
  "name": "@city/sim-core",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "pnpm -w tsc -p packages/sim-core/tsconfig.json --noEmit",
    "lint": "pnpm -w eslint packages/sim-core/src packages/sim-core/vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -w vitest run --root packages/sim-core",
    "test-parity": "CITY_TEST_PARITY=1 pnpm -w vitest run --root packages/sim-core",
    "test:watch": "pnpm -w vitest --root packages/sim-core"
  },
  "devDependencies": {
    "@city/micropolis-c-harness": "workspace:*",
    "@types/node": "25.0.10",
    "fast-check": "catalog:"
  }
}
