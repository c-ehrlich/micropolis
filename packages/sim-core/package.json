{
  "name": "@city/sim-core",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "pnpm -w tsc -p tsconfig.json --noEmit",
    "lint": "pnpm -w eslint src vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -w vitest run --root packages/sim-core",
    "test:watch": "pnpm -w vitest --root packages/sim-core"
  },
  "devDependencies": {
    "@types/node": "25.0.10",
    "fast-check": "catalog:"
  }
}
