import './index';

import { describe, expect, it } from 'vitest';

describe('@city/sim-scripting package scaffold', () => {
  it('loads the package entrypoint', () => {
    expect(true).toBe(true);
  });
});
